// Package config parses and validates rules.yaml (DESIGN.md Decision 4):
// typed filter/transform fields, CEL `when` predicates compiled and
// type-checked at startup, and Go templates for string transforms.
package config
